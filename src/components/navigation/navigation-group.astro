---
import NavigationLink from './navigation-link.astro';
import type { Link } from './navigation.astro';

const { links } = Astro.props;
---

<ul class='flex space-x-8'>
  {links.map((link: Link) => <NavigationLink {...link} />)}
</ul>
